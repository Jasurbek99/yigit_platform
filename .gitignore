# Excel data files (large, contains business data)
data/

# Python
backend/venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
.playwright-mcp/
# Django
backend/static/
backend/media/
*.sqlite3

# Node
frontend/node_modules/
frontend/dist/
frontend/.cache/

# Environment
.env
.env.local
.env.production

# Credentials (local dev only — never commit)
CREDENTIALS.md
docs/TEST_ACCOUNTS.md

# IDE
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Local Claude Code settings (user-specific)
.claude/settings.local.json

# Tooling artifacts that shouldn't ship
.tmp-i18n-backup/
backend/walk_shipment.py
# Superpowers brainstorm runtime state (pids, tokens, ports)
.superpowers/

# Root-level node_modules — only frontend/node_modules was covered before, so
# vitest's cache (node_modules/.vite/.../results.json) was tracked and churned
# on every test run.
/node_modules/

# Codex tooling config (mirrors .claude/). Left out of git rather than deleted —
# say the word if these should be committed for the whole team instead.
.codex/
AGENTS.md
.agents/

# Row-level DB backups taken before destructive data fixes. Production data — never commit.
backups/
