import type { IDemandItem } from '@/types';

/**
 * Mock demand data for the AssignmentBoard.
 * TODO: wire to real contract + quota endpoints when available.
 *
 * NOTE: `label` uses i18n keys — resolved in the component via t().
 */
export const MOCK_DEMAND: IDemandItem[] = [];
