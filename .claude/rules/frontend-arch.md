# Frontend Architecture Rules — moved

These rules now live in [`frontend/CLAUDE.md`](../../frontend/CLAUDE.md) so they load only
when Claude is working under that directory, instead of in every session.

Content is unchanged — see the `Frontend Architecture Rules` section there.
