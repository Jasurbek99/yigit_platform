# Clean Code Rules — Python/Django Backend — moved

These rules now live in [`backend/CLAUDE.md`](../../backend/CLAUDE.md) so they load only
when Claude is working under that directory, instead of in every session.

Content is unchanged — see the `Clean Code Rules` section there.
