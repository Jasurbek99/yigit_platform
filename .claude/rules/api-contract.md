# API Contract Rules — moved

These rules now live in the **`api-contract` skill** at
[`.claude/skills/api-contract/SKILL.md`](../skills/api-contract/SKILL.md), so they load on demand
instead of in every session.

Content is unchanged. Claude: invoke the `api-contract` skill before designing or changing an endpoint.
