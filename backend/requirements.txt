# YGT Platform — Backend Dependencies

# Core
Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4

# Database
mssql-django==1.7.1
pyodbc>=5.1,<5.2

# Config
python-dotenv>=1.0,<2.0

# HTTP client (Traccar GPS integration)
requests>=2.31,<3.0

# API
django-filter>=24.1,<25.0
django-cors-headers>=4.4,<4.5

# Security — brute-force login lockout (escalating: 3 fails -> 30m -> 5h -> 1d)
django-axes>=7.1,<8.0

# Data migration
openpyxl>=3.1,<3.2

# Document generation
python-docx>=1.1,<1.2
docxtpl>=0.19,<0.20         # Jinja-in-Word template fill (contract/invoice docs)
WeasyPrint>=62.0,<63.0
reportlab>=4.4,<5.0
# NOTE: PDF export of generated .docx requires LibreOffice headless on the
# server (soffice/libreoffice on PATH, or LIBREOFFICE_BIN setting). Not a pip dep.

# Production
gunicorn>=22.0,<23.0
redis>=5.0,<6.0

# Monitoring / error tracking
sentry-sdk[django]>=2.0,<3.0

# Realtime (WebSocket — Phase 1 plumbing)
# Production runs uvicorn workers under gunicorn (see Dockerfile) so /api/
# HTTP and /ws/ WebSocket share one port. daphne is also a runtime dep —
# it backs `manage.py runserver` in dev (via Channels' runserver hook) and
# channels.testing in CI. channels-redis uses the existing redis service.
channels>=4.1,<5
channels-redis>=4.2,<5
uvicorn[standard]>=0.30,<0.40
daphne>=4.1,<5

# Dev/test
pytest-django>=4.8,<4.9
factory-boy>=3.3,<3.4

# Async task scheduling (Traccar Fleet Map polling)
celery>=5.4,<5.5

# Deferred (Sprint 3+)
# python-telegram-bot>=21.0,<22.0
