# YGT Platform — Backend Dependencies

# Core
Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4

# Database
mssql-django==1.7.1
pyodbc>=5.1,<5.2

# Config
python-dotenv>=1.0,<2.0

# API
django-filter>=24.1,<25.0
django-cors-headers>=4.4,<4.5

# Data migration
openpyxl>=3.1,<3.2

# Document generation
python-docx>=1.1,<1.2
WeasyPrint>=62.0,<63.0
reportlab>=4.4,<5.0

# Production
gunicorn>=22.0,<23.0
redis>=5.0,<6.0

# Realtime (WebSocket — Phase 1 plumbing)
# uvicorn workers under gunicorn handle both /api/ HTTP and /ws/ WebSocket
# on the same port. channels-redis uses the existing Redis service as the
# channel layer backing so presence/broadcast survives multi-worker setups.
channels>=4.1,<5
channels-redis>=4.2,<5
uvicorn[standard]>=0.30,<0.40

# Dev/test
pytest-django>=4.8,<4.9
factory-boy>=3.3,<3.4
# daphne is only used by channels.testing (ChannelsLiveServerTestCase). The
# production stack runs on uvicorn workers under gunicorn — see Dockerfile.
daphne>=4.1,<5

# Deferred (Sprint 3+)
# celery>=5.4,<5.5
# python-telegram-bot>=21.0,<22.0
